---
import type { CollectionEntry } from "astro:content";
import BaseLayout from "./BaseLayout.astro";

interface Props {
  page: CollectionEntry<"pages">;
  class?: string;
}

const { page, class: className } = Astro.props;
---

<BaseLayout entity={page} class={className}>
  <slot />
</BaseLayout>
