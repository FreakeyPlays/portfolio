---
interface Props {
  label: string;
}

const { label } = Astro.props;
---

<div
  class="p-px overflow-hidden rounded-full bg-gradient-to-tr from-zinc-800 via-zinc-500 to-zinc-800"
>
  <div class="bg-zinc-800 h-full rounded-full px-2 text-sm font-semibold">{label}</div>
</div>
