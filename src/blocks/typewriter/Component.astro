---
import Typewriter from 'astro-typewriter';

interface Props {
  start: string;
  messages: string[];
  tagline: string;
}

const { start, messages , tagline } = Astro.props;

const longest = messages.reduce(
  (longestSoFar, message) => (message.length > longestSoFar.length ? message : longestSoFar),
  '',
);
---

<div>
	<h1 class="relative font-serif font-extrabold">
		<span aria-hidden="true" class="mb-2 block invisible">{start}&nbsp;{longest}</span>
		<span class="absolute inset-x-0 bottom-0 block"
		>{start}&nbsp;<Typewriter
			class="cursor bg-linear-to-br from-lime-400 to-green-600 bg-clip-text text-transparent"
			messages={messages}
		/></span
		>
	</h1>
	<p class="font-serif text-heading-05 font-medium text-gray-400">{tagline}</p>
</div>
